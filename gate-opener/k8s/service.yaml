apiVersion: v1
kind: Service
metadata:
  name: gate-opener
  labels:
    app: gate-opener
spec:
  type: ClusterIP
  selector:
    app: gate-opener
  ports:
    - name: http
      port: 8080
      targetPort: http
